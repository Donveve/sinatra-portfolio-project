<h1>Hello <%= current_student.name %></h1>
<br />
<p>
  Today is <%= @current_time.strftime("%A") %>, <%= @current_time.strftime("%B") %> <%= @current_time.strftime("%d") %>, <%= @current_time.strftime("%Y") %> <br />
</p>

<% if !current_student.subjects.empty? %>
  <h3>You have completed:</h3><br />
  <% current_student.subjects.each do |subject| %>
    <%= subject.name %>
    <br />
  <% end %>
<% else %>
  <p>You haven't finished any subjects today.</p>
<% end %>

<% if !current_student.instruments.empty? %>
  <h3>Today you practiced:</h3><br />
  <% current_student.instruments.each do |instrument| %>
    <%= instrument.name %>
    <br />
  <% end %>
<% else %>
    <p>You haven't practiced any instruments today</p>
<% end %>
<br /><br />
<a href="/students/<%=current_student.id%>/edit">Need to make corrections or additions?</a>
<br /><br />
<form method="get" action='/students/logout'>
  <input type='submit' id='signout' value='Sign Out' name='signout' />
</form>
<br />
<form method="get" action="/students/<%=current_student.id%>/delete">
  <input id="delete" type="submit" name="delete" value="Want to delete your account?">
</form>
